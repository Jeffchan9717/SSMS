{% extends "layout.html" %}
{% block body %}

{% if not session.logged_in %} 
{% for message in get_flashed_messages() %}
<br>
<div class="alert alert-info">
  <a href="/login" class="close" data-dismiss="alert">&times;</a>
  <br>
  <strong>注意!</strong>{{message}}
</div>
{% endfor %}
{% endif %}

<div class="container" style="margin-top: 10px">
<table id="view-table" cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>学号</th>
            <th>姓名</th>
            <th>性别</th>
            <th>出生日期</th>
            <th>籍贯</th>
            <th>院系</th>
            <th>专业</th>
            <th>入学年份</th>
        </tr>
    </thead>
    <tbody>
        {% for record in feedback %}
        <tr>
            <td>{{ record.sID }}</td>
            <td>{{ record.sName }}</td>
            <td>{{ record.sSex }}</td>
            <td>{{ record.sBirthday }}</td>
            <td>{{ record.sBirthPlace }}</td>
            <td>{{ record.sCollege }}</td>
            <td>{{ record.sMajor }}</td>
            <td>{{ record.sEnroll }}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

</div>



{% endblock %}
